#include <stdio.h>
/**
 * main - Serves as the entry point for the program
 * Return: By default returns zero if no errors otherwise
 */
int main(void)
{
	pusts("\"Programming is like building a multilingual puzzle);
	return (0);
}
